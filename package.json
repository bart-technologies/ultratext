{
  "name": "ultratext",
  "version": "0.1.0",
  "description": "UltraText Markup Language - runtime HTML templating",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
